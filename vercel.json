{
  "buildCommand": "npm run build",
  "framework": "nextjs",
  "installCommand": "npm install && prisma generate",
  "env": {
    "SKIP_ENV_VALIDATION": "1"
  },
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
